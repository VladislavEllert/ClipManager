# Текущие задачи

## ✅ Сделано

- Скелет проекта, menu bar агент, фон без Dock, single-instance guard.
- Захват буфера: текст, RTF, изображения, файлы (опрос `changeCount`, App Nap off, таймер `.common`).
- Хранилище на диске (JSON + PNG-блобы), дедуп, лимит 1–100, закрепление (pin).
- Глобальный хоткей `⌘⇧V` (Carbon) + рекордер смены хоткея.
- Плавающее изменяемое окно (Liquid Glass), запоминает размер.
- Навигация ↑/↓/Enter/Esc, `⌥Enter` — вставка без формата, контекст-меню (закрепить/удалить).
- Авто-вставка через `CGEvent` (реальные события Cmd, гейт Accessibility).
- Окно настроек: лимит, рекордер хоткея, автозагрузка, флаги фич.
- Поиск, избранное, пропуск паролей, переключатели фич + шестерёнка прямо в окне.
- Обновление окна при открытии (пересоздание контента) + live-refresh при открытом окне (`store.onChange`).
- README (стиль InvestorPro: badges, секции, дерево), git-репо, public, теги/keywords.

## ⏳ Ждёт локального подтверждения пользователя

- **Авто-вставка в iPhone Mirroring** (commit `6b1c9a2`): реальные события Cmd down/up
  вместо флага + задержка фокуса 0.08→0.15с. Симптом был: печаталась «м» (= V без Cmd
  в русской раскладке). Пользователь ещё не подтвердил, что починилось.

## 🔜 Следующее

- См. [backlog.md](backlog.md).
